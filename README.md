# Skyline

To compile this program use the makefile included in the src folder

To run the OpenMP program:

```bash
./omp-skyline < your_input_file > your_output_file
```

To run the CUDA program:

```bash
./cuda-skyline < your_input_file > your_output_file
```
